<%= form_for(@slide_item) do |f| %>
  <% if @slide_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@slide_item.errors.count, "error") %> prohibited this slide_item from being saved:</h2>

      <ul>
      <% @slide_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "标题" %>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label "轮播图片" %>
    <%= f.kindeditor :slideimg %>
  </div>
  <div class="field">
    <%= f.label "内容" %>
    <%= f.kindeditor :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
